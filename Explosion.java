import greenfoot.*;

public class Explosion extends Actor
{
    public Explosion() 
    {
        getImage().scale(getImage().getWidth() - 500, getImage().getHeight() - 500);
        setImage(getImage());
    }    
}
